#include <iostream>
#include <fstream>

using namespace std;

int main()
{
	fstream ipt;
	ipt.open("input.txt");
	
	int n, a, c, cnt;
	cin >> n;

	for (int i = 0; i < n; i++) {
		cin >> a;
		
		if (a = 1 && c = a)cnt++;;
		c = a;
	}
}